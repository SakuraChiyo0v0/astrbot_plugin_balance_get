{
    "admin_only": {
        "description": "👮 仅管理员可用",
        "type": "bool",
        "default": true,
        "hint": "开启后，只有在 AstrBot 全局配置中设置的管理员才能使用 /balance 指令。"
    }
}