{
    "admin_only": {
        "description": "👮 仅管理员可用",
        "type": "bool",
        "default": true,
        "hint": "开启后，只有在 AstrBot 全局配置中设置的管理员才能使用 /balance 指令。"
    },
    "show_unsupported": {
        "description": "👁️ 显示未适配平台",
        "type": "bool",
        "default": true,
        "hint": "在 /所有余额查询 中，是否在末尾列出未适配的平台名称。"
    }
}