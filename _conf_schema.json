{
    "admin_only": {
        "description": "👮 仅管理员可用",
        "type": "bool",
        "default": true,
        "hint": "开启后，只有在 AstrBot 全局配置中设置的管理员才能使用 /balance 指令。"
    },
    "show_unsupported": {
        "description": "👁️ 显示未适配平台",
        "type": "bool",
        "default": true,
        "hint": "在 /所有余额查询 中，是否在末尾列出未适配的平台名称。"
    },
    "output_template": {
        "description": "🎨 单项内容模板",
        "type": "text",
        "default": "🟢 **{{source_name}}**\n   💵 {{balance}} {{currency}}",
        "hint": "可用变量：{{source_name}}(平台), {{currency}}(币种), {{balance}}(智能余额), {{total_balance}}(总额), {{remaining_balance}}(剩余), {{used_balance}}(已用), {{raw_info}}(备注)。"
    },
    "header_template": {
        "description": "🏷️ 标题模板",
        "type": "text",
        "default": "💰 **{{title}}**",
        "hint": "消息头部的格式。可用变量：{{title}} (如“当前余额查询”)。标题下方会自动添加分隔符。"
    },
    "separator_template": {
        "description": "➖ 分隔符模板",
        "type": "text",
        "default": "\n━━━━━━━━━━━━━━\n",
        "hint": "标题与内容、以及多项结果之间的分隔符。支持 \\n 换行。"
    }
}